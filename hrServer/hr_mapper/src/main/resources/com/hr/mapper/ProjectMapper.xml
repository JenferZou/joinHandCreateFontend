<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hr.mapper.ProjectMapper">
    <select id="getAllProject" resultType="com.hr.model.Project">
        select id, resNumber, nowNumber, name, mentor, startTime, content, needCount, needMajor from project
    </select>
<delete id="deleteOneById">
    delete from project where id=#{id}
</delete>
    <select id="queryProjectByTitle" resultType="com.hr.model.Project">
        select resNumber, nowNumber, name, mentor, startTime, content, needCount, needMajor, id from project
where name=#{title}
    </select>
</mapper>